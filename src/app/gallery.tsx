import { Gallery } from '@/ui/gallery';

/**
 * `kolvi://gallery` — the KMO-3 primitives on a device. Scaffolding; KMO-30
 * deletes it along with `src/ui/gallery.tsx`.
 */
export default function GalleryRoute() {
  return <Gallery />;
}
